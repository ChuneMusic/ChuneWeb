import React from 'react';
import LinearProgress from '@material-ui/core/LinearProgress';

export const Loading = () => (
  <LinearProgress color="primary" style={{ width: '100%', margin: '0 0 20px 0' }} />
);
